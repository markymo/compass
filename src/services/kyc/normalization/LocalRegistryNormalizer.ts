import { FieldCandidate } from "./types";

export function mapLocalRegistryPayloadToFieldCandidates(payload: any, evidenceId: string): FieldCandidate[] {
    // Placeholder for Companies House or other local registry mapping
    return [];
}
